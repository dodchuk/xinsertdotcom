import React from 'react';
import { Metadata } from 'next';
import Snap from '@/ui/Pages/Private/Snap';

export const metadata: Metadata = {
  title: 'X Insert / Під замочком',
};

export default function Page() {
  return (
    <Snap/>
  );
}
